language: php

php: [7.2, 7.3]

branches:
  except:
    - /^bugfix\/.*$/
    - /^feature\/.*$/
    - /^optimization\/.*$/

before_script:
  - composer install --dev --prefer-source

script:
  - vendor/bin/phpunit
